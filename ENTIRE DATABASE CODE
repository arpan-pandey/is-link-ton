-- create a database named "islinkton" and paste each one of these codes to create the tables ---

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    full_name VARCHAR(100) NOT NULL,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    role ENUM('Student', 'Faculty', 'Admin') NOT NULL DEFAULT 'Student',
    profile_image VARCHAR(255) DEFAULT NULL,
    is_approved BOOLEAN DEFAULT FALSE,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

---

INSERT INTO users (full_name, username, email, password, role, is_approved, is_active)
VALUES 
    ('System Administrator', 'Admin', 'admin@islinkton.edu', '$2a$12$wfoCoDeEx.uFpt/fUasT6eDh84mF.qKOtPDKNVhQEG6FVzWVeHQa.', 'Admin', TRUE, TRUE), -- password is "password"
    ('Module Teacher', 'Teacher', 'teacher@islinkton.edu', '$2a$12$wfoCoDeEx.uFpt/fUasT6eDh84mF.qKOtPDKNVhQEG6FVzWVeHQa.', 'Faculty', TRUE, TRUE), -- password is "password"
    ('John Student', 'Student', 'student@islinkton.edu', '$2a$12$wfoCoDeEx.uFpt/fUasT6eDh84mF.qKOtPDKNVhQEG6FVzWVeHQa.', 'Student', TRUE, TRUE); -- password is "password"

---

CREATE TABLE categories (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL UNIQUE,
    type ENUM('Thread','Petition','Course')
);

---

INSERT INTO categories (name, type) VALUES
	('General Discussion', 'Thread'),
	('Academic Help', 'Thread'),
	('Announcements', 'Thread'),
	('Events', 'Thread'),
	('College Facilities', 'Petition'),
	('Academic Policy', 'Petition'),
	('Student Life', 'Petition'),
	('Computing', 'Course'),
	('Networking', 'Course'),
	('Cybersecurity', 'Course'),
	('Multimedia', 'Course'),
	('AI', 'Course'),
	('BBA - International Business', 'Course'),
	('BBA - Digital Business Management', 'Course'),
	('BBA - Advertising and Marketing', 'Course'),
	('BBA - Events & Tourism Management', 'Course'),
	('BBA - Accounting & Finance', 'Course');

---

CREATE TABLE threads (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    category_id INT,
    author_id INT,
    is_approved BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL,
    FOREIGN KEY (author_id) REFERENCES users(id) ON DELETE SET NULL
);

---

INSERT INTO threads (title, content, category_id, author_id, is_approved) VALUES
('Midterm Exams Routine', 'Greetings Students!, Please find the timetable of the exam below:', 3, 2, TRUE),
('How to prepare for upcoming midterm exams?', 'Any tips for time management and resources?', 2, 3, TRUE);

---

CREATE TABLE thread_votes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    thread_id INT NOT NULL,
    user_id INT NOT NULL,
    UNIQUE KEY unique_user_vote (thread_id, user_id),
    FOREIGN KEY (thread_id) REFERENCES threads(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

---

INSERT INTO thread_votes (thread_id, user_id) VALUES
(1, 1),
(1, 2),
(2, 2);

---

CREATE TABLE posts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    thread_id INT NOT NULL,
    user_id INT,
    content TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (thread_id) REFERENCES threads(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL
);

---

INSERT INTO posts (thread_id, user_id, content) VALUES
(1, 3, 'This routine seems very random.'),
(2, 2, 'Make a proper timetable and revise daily.');

---

CREATE TABLE resources (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    description TEXT,
    file_path VARCHAR(255) NOT NULL,
    file_type VARCHAR(255) NOT NULL,
    uploaded_by INT NOT NULL,
    module_name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (uploaded_by) REFERENCES users(id) ON DELETE CASCADE
);

---

CREATE TABLE petitions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    created_by INT NOT NULL,
    is_approved BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE
);

---

INSERT INTO petitions (title, content, created_by, is_approved) VALUES
('Extend Library Hours During Exam Week', 'Current library timing is insufficient during exams.', 3, TRUE),
('Improve WiFi in Skill Block', 'WiFi connectivity is very poor in the Skill Block.', 3, TRUE);

---

CREATE TABLE petition_votes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    petition_id INT NOT NULL,
    user_id INT NOT NULL,
    UNIQUE KEY unique_petition_vote (petition_id, user_id),
    FOREIGN KEY (petition_id) REFERENCES petitions(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

---

INSERT INTO petition_votes (petition_id, user_id) VALUES
(1, 1),
(1, 2),
(2, 3);

---

CREATE TABLE study_groups (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(150) NOT NULL,
    description TEXT,
    category_id INT,
    whatsapp_link VARCHAR(255),
    created_by INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL,
    FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE SET NULL
);

---

INSERT INTO study_groups (name, description, category_id, whatsapp_link, created_by) VALUES
('CS301 Algorithm Study Group', 'Weekly discussions on DP and Graph problems', 12, 'https://chat.whatsapp.com/abc123', 2),
('BBA Digital Marketing Group', 'Case studies and project help', 15, 'https://chat.whatsapp.com/xyz789', 3);